apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-red
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
        color: red
    spec:
      containers:
      - name: simple-web
        image: bubri.azurecr.io/simple_web
        ports:
        - containerPort: 80